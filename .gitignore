# Private planning docs: tracked in the internal Forgejo repo only, never in
# git, never on the public GitHub mirror. Excluded before the first commit.
docs/

# Fetched or generated, never committed
testdata/vectors/
testdata/corpus/
tools/oracle/build/
tools/oracle/dumps/
tools/oracle/mutated/
tools/quality/out/

# go test -fuzz corpora (per-package testdata/fuzz/<FuzzName>/), including
# any failing inputs it saves locally; the seed corpus lives in code via
# f.Add, not on disk.
**/testdata/fuzz/

# Build artifacts
/bin/
/quality
*.exe
*.test
*.out

# Coverage and profiling
coverage.out
coverage.txt
*.cov
*.prof
*.pprof

# Agent and tooling scratch (local only)
.superpowers/
.serena/
.codegraph/
cache/

# Local-only assistant notes: private tooling and host setup, never commit
CLAUDE.local.md
*.local.md

# Editor and OS noise
.DS_Store
*.swp
.idea/
.vscode/
